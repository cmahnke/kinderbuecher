{
  "dependencies": {
    "jquery": "^3.5.1",
    "mirador": "ProjectMirador/mirador"
}, "devDependencies": {
      "svgo": "^1.3.2",
      "patch-package": "^6.2.2",
      "react": "^17.0.1",
      "react-dom": "^17.0.1",
      "url": "^0.11.0"
    },
    "scripts": {
      "postinstall": "yarn run patch-package && cd node_modules/mirador && yarn install && yarn run build && yarn run build:es"
    }
}
